<!DOCTYPE html>
<html>
<head>
  <title>FS Digital Visitor Registry Book</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>

  <!-- font awesome -->
  <link href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.css" rel="stylesheet">

</head>
<body>

  <div>
    <div class="panel panel-default" style="background-color:#d8d6d6;">
      <div class="panel-body text-right">
        <% if admin_signed_in? %>
            Currently siged in as: <strong><%= current_admin.email %></strong> |
            <span class="btn-group">
              <%= link_to "edit password", edit_admin_registration_path, method: :get, class: "btn btn-primary btn-sm" %>
              <%= link_to "Sign out", destroy_admin_session_path, method: :delete, class: "btn btn-danger btn-sm" %>
            </span>
        <% end %>
      </div>
    </div>
  </div>

  <div class="container-fluid">
    <div class="row">
      <div class="col-md-8">
        <%= yield %>
      </div>

      <div class="col-md-4">
        <%= yield :filteringSection %>
      </div>
    </div>
  </div>

</body>
</html>
